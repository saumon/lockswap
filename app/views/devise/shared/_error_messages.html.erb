<% if resource.errors.any? %>
  <div id="error_explanation" data-turbo-temporary role="alert"
       class="mb-6 rounded-md border border-rose-200 bg-rose-50 px-4 py-3">
    <h2 class="text-sm font-semibold text-rose-900">
      <%= I18n.t("errors.messages.not_saved",
                 count: resource.errors.count,
                 resource: resource.class.model_name.human.downcase) %>
    </h2>
    <ul class="mt-2 list-outside list-disc space-y-1 pl-5 text-sm text-rose-800">
      <% resource.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>
