# Phase 1 Data Model: Compact floor/locker label alignment

**Not applicable.** This feature changes only how already-existing data
(`current_user.saved_floor`, `current_user.saved_locker_number`,
`current_user.locker_wish.saved_floor`) is arranged and styled on screen. No
entity, attribute, validation rule, or state transition is added, removed, or
changed. See `spec.md` (no "Key Entities" section was included, since the
feature involves no data changes) and `plan.md`'s Technical Context
(`Storage: N/A`).
